[project]
name = "mongkdo-backend"
version = "0.1.0"
description = "Socket approval backend (Telethon + FastAPI)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "python-socketio>=5.11.0",
    "telethon>=1.37.0",
    "pydantic>=2.0.0",
]
